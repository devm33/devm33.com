# Site settings
title: Devraj Mehta
email: devm33@gmail.com
description: > # this means to ignore newlines until "baseurl:"
    Developer of Software, Scientist of Computers, Builder of Websites.
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://devm33.com" # the base hostname & protocol for your site

s3url: "http://devm33.s3.amazonaws.com"

twitter_username: devm33
github_username:  devm33

# Build settings
markdown: kramdown
highlighter: pygments
sass:
    style: :compressed
permalink: /:year-:month-:day/
include: [".htaccess"]

# yaml front matter defaults
defaults:
    -
        scope:
            path: "" # defaults for all pages
        values:
            layout: "post"

# Plugin settings
safe: true # true for no sym links or custom plugins (github pages uses safe)

