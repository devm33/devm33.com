legacy-peer-deps=true
; Refuse packages whose version was published less than 3 days ago to
; mitigate supply-chain attacks from freshly-published malicious/compromised
; releases. Requires npm >= 11.5.1. Value is in days.
min-release-age=3
